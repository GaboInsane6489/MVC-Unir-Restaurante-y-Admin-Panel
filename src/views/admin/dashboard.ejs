<!DOCTYPE html>
<html lang="es">
    <%- include("../partials/head") %>
    <body class="bg-gray-100 p-6">
        <div class="mb-6 flex justify-between">
            <a href="/" class="text-blue-500 hover:underline">Inicio</a>
            <a href="/manage" class="text-blue-500 hover:underline">Gestión</a>
            <a href="/admin/menu" class="text-blue-500 hover:underline">Menú</a>
        </div>

        <h1 class="text-3xl font-bold text-center mb-6 text-[#F26419]">Admin Dashboard</h1>

        <div class="grid grid-cols-3 gap-6 text-white font-bold text-xl">
            <div class="bg-[#F26419] p-6 rounded-md text-center">
                Por hacer: <%= counts.todo %>
            </div>
            <div class="bg-[#F6AE2D] p-6 rounded-md text-center">
                Completadas: <%= counts.done %>
            </div>
            <div class="bg-[#86BBD8] p-6 rounded-md text-center">
                Entregadas: <%= counts.delivered %>
            </div>
        </div>

        <form action="/admin/cleanup" method="POST" class="mt-8 text-center">
            <button class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">
                Limpiar Entregadas
            </button>
        </form>
    </body>
</html>
